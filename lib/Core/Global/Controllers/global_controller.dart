import '../../Base/base_controller.dart';
import '../Cores/global_repository.dart';

class GlobalController extends BaseController {
  final GlobalRepository globalRepo;

  GlobalController(this.globalRepo);
}
